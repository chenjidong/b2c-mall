<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ppepper.coupon.mapper.CouponMapper">

    <update id="decCoupon">
        update b2c_coupon set surplus = surplus - 1 where id = #{couponId}
    </update>

    <select id="getActiveCoupons" resultType="com.ppepper.common.dto.CouponDTO">
        SELECT
            c.id,
            c.title,
            c.type,
            c.description,
            c.total,
            c.surplus,
            c.`limit`,
            c.discount,
            c.`min`,
            c.`status`,
            c.category_id AS categoryId,
            c.days,
            c.gmt_start AS gmtStart,
            c.gmt_end AS gmtEnd,
            c.gmt_update AS gmtUpdate,
            c.gmt_create AS gmtCreate,
        FROM
            b2c_coupon AS c
        WHERE
            c.`status` = 1
            AND (c.gmt_end IS NULL OR ( unix_timestamp(c.gmt_start) &lt; unix_timestamp(now( )) AND unix_timestamp(c.gmt_end) &gt; unix_timestamp(now( )) ))
    </select>


</mapper>